// Single source of truth for the app version. Shown in Settings so you can always
// tell which build your device is running, and bumped on every deploy. Keep the
// sw.js CACHE_NAME bumped in lockstep so the service worker re-caches new assets.
export const APP_VERSION = '2.3.0';
